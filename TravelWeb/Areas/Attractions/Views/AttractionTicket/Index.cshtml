@model IEnumerable<TravelWeb.Areas.Attractions.Models.AttractionProduct>

@{
    ViewData["Title"] = "景點票券管理 - 終極整合版";
}

<div class="page-inner">
    <div class="page-header">
        <h4 class="page-title">票券產品管理</h4>
        <ul class="breadcrumbs">
            <li class="nav-home"><a href="#"><i class="flaticon-home"></i></a></li>
            <li class="separator"><i class="flaticon-right-arrow"></i></li>
            <li class="nav-item"><a href="#">景點區</a></li>
            <li class="separator"><i class="flaticon-right-arrow"></i></li>
            <li class="nav-item"><a href="#">票券管理</a></li>
        </ul>
    </div>

    <div class="row">
        <div class="col-md-12">
            <div class="card">
                <div class="card-header d-flex align-items-center">
                    <h4 class="card-title">票券列表</h4>
                    <a asp-action="Create" class="btn btn-primary btn-round ms-auto">
                        <i class="fa fa-plus"></i> 新增票券
                    </a>
                </div>
                <div class="card-body">
                    <div class="table-responsive">
                        <table class="table table-hover align-middle">
                            <thead class="table-light">
                                <tr>
                                    <th>產品代碼</th>
                                    <th>票券名稱</th>
                                    <th>所屬景點</th>
                                    <th>售價</th>
                                    <th>系統狀態 (點擊切換)</th>
                                    <th>銷售狀態 (點擊切換)</th>
                                    <th style="width: 15%">操作</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td><code class="text-primary">@item.ProductCode</code></td>
                                        <td><strong>@item.Title</strong></td>
                                        <td>@(item.Attraction?.Name ?? "未指定")</td>
                                        <td class="text-danger fw-bold">@item.Price?.ToString("C0")</td>
                                        <td>
                                            @* 點擊變更 DRAFT/PUBLISHED/ARCHIVED *@
                                            <button type="button" class="btn btn-xs btn-outline-info btn-round"
                                                    onclick="changeStatus(@item.ProductId, '@(item.Status?.ToUpper() ?? "DRAFT")', '@item.Title')">
                                                <i class="fas fa-sync-alt"></i> @(item.Status ?? "DRAFT")
                                            </button>
                                        </td>
                                        <td>
                                            @* 點擊變更 0/1 *@
                                            @if (item.IsActive == 1)
                                            {
                                                <button type="button" class="btn btn-xs btn-success btn-round"
                                                        onclick="toggleActive(@item.ProductId, 0, '@item.Title')">
                                                    銷售中
                                                </button>
                                            }
                                            else
                                            {
                                                <button type="button" class="btn btn-xs btn-secondary btn-round"
                                                        onclick="toggleActive(@item.ProductId, 1, '@item.Title')">
                                                    已下架
                                                </button>
                                            }
                                        </td>
                                        <td>
                                            <div class="btn-group">
                                                <a asp-action="Edit" asp-route-id="@item.ProductId" class="btn btn-sm btn-outline-primary"><i class="fas fa-edit"></i></a>
                                                <a asp-controller="Inventory" asp-action="Details" asp-route-id="@item.ProductId" class="btn btn-sm btn-outline-info"><i class="fas fa-boxes"></i></a>
                                                <button type="button" class="btn btn-sm btn-outline-danger" onclick="confirmDelete(@item.ProductId, '@item.Title')"><i class="fas fa-trash"></i></button>
                                            </div>
                                            <form id="delete-form-@item.ProductId" asp-action="Delete" asp-route-id="@item.ProductId" method="post" style="display:none;">
                                                @Html.AntiForgeryToken()
                                            </form>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // 封裝表單提交，減少重複代碼
        function quickPost(action, data) {
            const form = document.createElement('form');
            form.method = 'POST';
            form.action = action;

            // 加入 CSRF Token 防止 400 錯誤
            const token = document.querySelector('input[name="__RequestVerificationToken"]');
            if (token) form.appendChild(token.cloneNode());

            for (const key in data) {
                const input = document.createElement('input');
                input.type = 'hidden';
                input.name = key;
                input.value = data[key];
                form.appendChild(input);
            }
            document.body.appendChild(form);
            form.submit();
        }

        // 1. 銷售狀態切換
        function toggleActive(id, newStatus, title) {
            const text = newStatus === 1 ? '上架' : '下架';
            Swal.fire({
                title: `確定要${text}嗎？`,
                text: `票券「${title}」狀態將變更。`,
                icon: 'question',
                showCancelButton: true,
                confirmButtonText: `是的，${text}！`
            }).then((result) => {
                if (result.isConfirmed) {
                    quickPost('@Url.Action("ToggleActive")', { id: id, isActive: newStatus });
                }
            });
        }

               // 2. 系統狀態切換
        function changeStatus(id, current, title) {
            Swal.fire({
                title: '變更系統狀態',
                text: `票券「${title}」目前狀態為：${current}`,
                input: 'select',
                inputOptions: {
                    // 左邊的 Key 必須嚴格對應資料庫：ARCHIVED, INACTIVE, ACTIVE, DRAFT
                    'DRAFT': '草稿 (DRAFT)',
                    'ACTIVE': '已發佈 (ACTIVE)',   // 之前你傳 PUBLISHED，現在改傳 ACTIVE
                    'INACTIVE': '已下架 (INACTIVE)', // 之前你傳其他值，現在改傳 INACTIVE
                    'ARCHIVED': '封存 (ARCHIVED)'
                },
                inputValue: current.toUpperCase().trim(), // 確保預設選中項正確
                showCancelButton: true,
                confirmButtonText: '確定更新',
                cancelButtonText: '取消'
            }).then((result) => {
                if (result.isConfirmed && result.value) {
                    // 這裡傳送出去的 result.value 就會是 'ACTIVE' 或 'INACTIVE' 等
                    quickPost('@Url.Action("UpdateStatus")', { id: id, status: result.value });
                }
            });
        }

        // 3. 刪除確認
        function confirmDelete(id, name) {
            Swal.fire({
                title: '確定刪除？',
                text: name,
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#d33'
            }).then((result) => {
                if (result.isConfirmed) {
                    document.getElementById('delete-form-' + id).submit();
                }
            });
        }

        // 4. 成功訊息
        const successMsg = '@Html.Raw(TempData["SuccessMessage"])';
        if (successMsg) {
            Swal.fire({ icon: 'success', title: '完成', text: successMsg, timer: 2000, showConfirmButton: false });
        }
    </script>
}